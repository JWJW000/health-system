健身用户个性化饮食和训练系统的设计与实现

（设计）开题报告三、设计开发（研究）内容功能需求分析及设计框架一、功能需求分析1.用户基础信息填写功能用户根据自身情况填写年龄、性别、身高、体重、体脂率、健身目标（增肌/减脂/塑性/功能性训练）等核心信息，选填健身年限、训练场景、过敏食材等信息，支持用户信息修改与保存，数据保存数据库，用户修改后同步更新。支持用户记录每日体重，同时生成体重曲线图（15天曲线图），帮助用户直观了解体重变化。2.用户基础代谢与健身营养计算功能：用户可以根据自己填的信息，通过系统计算功能（男性基础代谢率=10×体重(kg)+6.25×身高(cm)-5×年龄+5，女性基础代谢率=10×体重(kg)+6.25×身高(cm)-5×年龄-161）、然后根据（增肌/减脂/塑性/功能性训练）目标，结合健身领域通用的营养配比规则去确定三大营养物质（碳水，蛋白质，脂肪）占比，为用户计算出（静息状态下）所需要的各类营养物质的克数。不同健身目标对应的营养配比规则为：减脂目标以高蛋白、低碳水为核心，增肌目标以高碳水、足量蛋白为核心，塑形目标以均衡配比为核心，功能性训练目标以兼顾体能与蛋白补充为核心，确保计算结果完全匹配用户的健身目标，为用户的日常饮食提供明确的数值参考。在用户修改体重，年龄，健身目标等核心信息后，系统进行重新计算。3饮食推荐功能：根据用户（增肌/减脂/塑性/功能性训练）目标去匹配项目中的食谱模板（如增肌高蛋白食谱、减脂低卡食谱、塑形均衡食谱、功能性训练体能食谱），每类食谱均提前完成热量与营养配比核算，确保与对应健身目标的营养需求相匹配，同时根据用户的训练目标展示出每日所需热量缺口/盈余，食谱含每日3餐+加餐的食材、用餐时间、用量，同时为用户出具一定的烹饪建议，让用户对饮食有更清晰的理解。4训练动作指导功能：分为无氧训练和有氧训练两部分。无氧训练该板块系统插入健身动作图片，健身动作视频和对这个动作的文字描述（包括该动作训练注意事项），根据训练的侧重点不同，对训练动作进行清晰的分类（练胸、
2026届中原工学院毕业论文（设计）开题报告练背、练腿、练肩、练核心等）用户根据今日训练可以直接点开不同的模块模仿动作，看讲解。会根据用户整体的计划（增肌/减脂）为用户安排训练日和休息日（练三休一、练四休一、练五休一），用户可在训练日自行选择今日的训练动作。同时，不同的动作会根据不同的训练要求分出来不同的训练组数、训练重量方面的要求。有氧训练该板块：系统基于用户的健身目标与无氧训练规划，为用户提供适配的有氧训练指导，核心包括推荐的有氧训练类型（如慢跑、跳绳、游泳、快走等）、单次训练时长、训练频率、心率要求。比如减脂目标推荐每次有氧训练 30-45 分钟，每周 2-3 次；增肌目标推荐控制有氧训练时长，避免肌肉消耗，同时给出有氧训练的时间安排建议，帮助用户将有氧与无氧训练合理结合，更快实现健身目标。5签到打卡功能：用户完成当日训练后，可返回系统首页，点击打卡按钮完成当日的训练签到，系统将自动记录用户的打卡日期与打卡时间，与用户账号绑定存储。系统在首页顶部设置累计打卡总数展示栏，实时统计并展示用户自注册以来的累计打卡总次数，让用户直观看到自己的训练坚持情况；同时系统支持查看历史打卡记录，展示用户的打卡日期列表，清晰还原用户的历史训练执行情况。二、设计框架1. 系统架构设计前端层：负责用户信息录入、数据可视化、方案展示与交互操作，采用友好的界面设计，提供直观便捷的操作体验。后端层：承载代谢计算、方案匹配、训练指导、打卡统计等核心业务模块，实现系统核心功能的稳定高效运行。数据层：负责存储和管理系统所需的全量数据资源，包括用户档案、食谱库、动作库、打卡记录与静态资源等。2. 模块划分与交互设计用户信息管理模块：负责采集、修改、存储用户基础信息与体重数据，实现体重变化趋势可视化，为后续业务提供核心数据支撑。基础代谢与营养计算模块：基于用户基础信息，通过标准化公式完成基础代谢率测算与三大营养素摄入量计算，输出科学营养管控标准。
2026届中原工学院毕业论文（设计）开题报告饮食推荐模块：结合用户健身目标与营养计算结果，匹配对应食谱模板，展示全时段饮食方案、烹饪建议与热量管控要求。训练动作指导模块：根据用户健身目标规划训练周期，分无氧、有氧两大板块提供标准化动作教学、训练参数推荐与方案指导。签到打卡模块：负责用户训练执行情况的打卡记录、数据统计与状态可视化，形成完整的健身执行闭环。3. 技术选型与实现路径前端开发技术：采用 Vue3 搭配 Element Plus 组件库、ECharts 实现页面开发与数据可视化，保障界面易用性与交互流畅度。后端开发技术：基于 SpringBoot 2.x 框架搭建后端单体服务，搭配 MyBatis-Plus 简化数据库操作，实现核心业务逻辑开发。数据存储技术：选用 MySQL 8.0 存储结构化业务数据，动作教学、食谱相关静态资源采用本地服务器存储，简化部署流程。系统优化与扩展技术：通过代码分层架构优化系统运行性能，提供标准化 RESTful API 接口，便于后续功能扩展与集成。
2026届中原工学院毕业论文（设计）开题报告四、设计开发技术（研究）基础系统设计实现的手段、开发环境或条件一、系统设计实现的手段1. 用户信息管理模块实现手段前端采用 Vue3 框架结合 Element Plus 的 el-form 组件构建信息录入表单，通过 rules 配置项实现年龄、身高、体重等数值型数据的非空与合法性校验（如身高设置 100-250cm 的合理阈值）；使用 Axios 库将表单数据以 POST 请求发送至后端 SpringBoot 服务。后端 Controller 层通过 @RequestBody 注解接收用户信息实体类，Service 层调用 MyBatis-Plus 的 insert 或 updateById 方法实现数据的持久化存储与实时更新，数据库表中设置 update_time 字段自动记录修改时间。体重数据可视化方面，前端通过 ECharts 的 line 类型图表实现 15 天体重曲线，调用后端接口获取按日期排序的体重历史数据后，使用 setOption 方法动态渲染图表，横轴配置为日期、纵轴为体重数值。2. 基础代谢与健身营养计算模块实现手段前端将用户性别、体重、身高、年龄、健身目标等参数通过 Axios 传递至后端计算接口。后端 Service 层采用 Java 代码实现 Mifflin-St Jeor 公式的分支逻辑，根据性别参数选择对应公式计算基础代谢率；通过 switch-case 语句匹配增肌、减脂、塑形、功能性训练四类健身目标，调用预设的营养配比常量（如减脂期蛋白质 2.0-2.2g/kg 体重），结合用户体重数值完成三大营养素克数的精准计算。使用 SpringBoot 的 @PostMapping 注解定义计算接口，返回包含基础代谢率、三大营养素摄入量的 JSON 对象给前端展示；同时在用户信息更新接口中嵌入计算逻辑，实现核心信息变更后的自动重新计算。3. 饮食推荐模块实现手段数据库中设计 diet_template 表存储食谱模板，通过 fitness_type 字段标记适配的健身目标，提前录入每类食谱的总热量、三大营养素含量、食材清单、烹饪建议等数据。后端 Service 层使用 MyBatis-Plus 的 QueryWrapper，根据用户当前的健身目标字段查询对应类别的食谱列表，返回给前端。前端采用 Element Plus 的 el-card 组件展示食谱详情，图片通过 img 标签加载本地服务器存储的资源路径；食谱收藏功能通过后端 diet_collect 表实现，设置 user_id 和 diet_id 的联合唯一索引防止重复收藏，前端点击
2026届中原工学院毕业论文（设计）开题报告收藏按钮时调用 Axios 请求，后端通过 save 或 removeById 方法完成收藏 / 取消收藏操作。4. 训练动作指导模块实现手段无氧训练部分，数据库 train_action 表通过 muscle_group 字段存储肌群分类（练胸、练背等），后端按该字段分组查询动作列表，前端使用 Element Plus 的 el-tabs 组件实现分类切换展示；动作的图片、视频资源存储在本地服务器，路径存入数据库的 image_url、video_url 字段，前端通过 video 标签播放教学视频、img 标签展示动作示意图；训练周期规划通过 Java 代码实现逻辑分支，如增肌目标返回 “练四休一” 的日期数组，前端根据数组渲染训练日 / 休息日标识。有氧训练部分，后端通过 if-else 判断用户健身目标，返回对应的训练类型、时长、频率等参数，前端直接展示推荐内容。5. 签到打卡模块实现手段前端点击首页打卡按钮时，通过 Axios 将当前用户 ID 和日期发送至后端打卡接口。后端 Service 层先使用 MyBatis-Plus 的 count 方法查询 check_in 表，判断该用户当日是否已有打卡记录（通过 user_id 和 check_date 字段匹配），若没有则调用 save 方法插入打卡数据，若有则返回 “今日已打卡” 的提示。累计打卡总数通过 count 方法统计该用户的所有打卡记录数，实时返回给前端展示；历史打卡记录通过 list 方法按日期倒序查询，前端用 el-table 组件展示日期列表。打卡日历采用 ECharts 的 calendar 组件实现，前端将打卡日期数组传给组件，通过 itemStyle 配置已打卡日期的样式标记。